# Project Overview

a brief description of what the application does

# Prerequisites

what needs to be installed before running (Docker, Docker Compose, etc.)

Docker is required to build and run this application. Please see platform specific instructions below.

## Windows

1. Download and install Docker Desktop.
    1. Ensure that WSL2 is also installed and running as well. 
2. Ensure Docker Desktop is actively running before attempting to build/run the application.

## Linux

1. Install Docker using your distro's package manager.


# Environment Setup

how to configure the `.env` file (list all required variables with descriptions, but no actual values)

# How to Run

step-by-step instructions to get the application running from a fresh clone

# How to Seed the Database

explain how seeding is handled automatically on first run

# Default Login Credentials

the seeded usernames and passwords for each role (customer, agent, admin) so the reviewer can log in immediately

# Chatbot Setup

any specific steps required to enable the AI chatbot

# Assumptions & Design Decisions

a short section explaining any notable choices you made or assumptions you relied on where requirements were not explicit