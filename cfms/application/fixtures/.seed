# here is where the seed fixtures will be stored.
# at the moment I will create the actual set of seed data at the end
# of the dev process.
# but I already created the groups and users initial to keep things moving.
# I will commit in the groups.json first